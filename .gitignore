# Secrets and local credentials — never commit
.env
.env.local
.env.*.local
*.env
launch_gui.local.ps1
**/agu_ieeglogin.bin
**/*password*.bin
**/*login*.bin

# Review outputs — keep secrets out of annotation paths if added later
# (clip review JSON under sz-gui/seizure_gui/annotations/ is tracked)

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
